import { DashboardVisitTable } from "../../../components";

const DashboardVisitPage = () => {
  return (
    <div className="p-6">
      <DashboardVisitTable isViewAll={true} />
    </div>
  );
};

export default DashboardVisitPage;
