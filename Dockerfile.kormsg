FROM node:18-alpine as build

WORKDIR /app
COPY . .
COPY .env.kormsg ./.env
RUN npm install 
RUN npm run build

## run stage ##
FROM nginx:alpine
# RUN mkdir /run
COPY --from=build /app/build /run
COPY nginx.conf /etc/nginx/nginx.conf